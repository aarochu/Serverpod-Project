class: ReviewNotification
table: code_butler_review_notification
fields:
  userId: String
  reviewSessionId: int
  type: String
  message: String
  read: bool
  createdAt: DateTime
indexes:
  - fields: [userId]
  - fields: [read]
  - fields: [createdAt]

