class: AppliedFix
table: code_butler_applied_fix
fields:
  findingId: int
  fixCode: String
  appliedAt: DateTime
  status: String
  prUrl: String?
indexes:
  - fields: [findingId]
  - fields: [status]

